pre-commit:
    commands:
        check:
            glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
            run: pnpm lint --no-error-on-unmatched-pattern --no-warn-ignored --no-color{staged_files}
            stage_fixed: true
