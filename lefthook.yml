pre-commit:
    commands:
        lint:
            glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
            run: pnpm lint --no-error-on-unmatched-pattern --no-warn-ignored --no-color{staged_files}
            stage_fixed: true
        # generate-machine-types:
        #     glob: "shared/*.{ts}"
        #     run: pnpm run generate:state-machine-types
        #     stage_fixed: true
