import { actionCardsByRole } from './actionCards';
import businessDealCards from './businessDealCards';
import { capitalistCompanies, cooperativeFarms, middleClassCompanies, stateCompanies } from './companyCards';
import eventCards from './eventCards';
import exportCards from './foreignMarketCards';
import immigrationCards from './immigrationCards';
import loanCards from './loanCards';
import politicalAgendaCards from './politicalAgendaCards';

export const baseGameDecks = {
	actionCardsByRole,
	businessDealCards,
	capitalistCompanies,
	cooperativeFarms,
	eventCards,
	exportCards,
	immigrationCards,
	loanCards,
	middleClassCompanies,
	politicalAgendaCards,
	stateCompanies,
};

export default baseGameDecks;
